export type Service1  = {
    // id:number;                          // 1 - realy need ?
    id_user:number;                     // 1
    id_service:string;                // msa
    list_user_role:string;         // '['msa_user', 'msa_admin']'
};

export type Service2  = {
    // id:number;                          // 1 - realy need ?
    id_user:number;                     // 1
    id_service:string;                // lpu
    list_user_role:string;         // '['lpu_user', 'lpu_admin']'
};
