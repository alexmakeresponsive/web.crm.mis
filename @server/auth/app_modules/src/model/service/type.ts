export type Service1  = {
    // id:number;                          // 1 - realy need ?
    id_user:number;                     // 1
    id_service:string;                // msa
    list_user_service:string;         // '['msa_user', 'msa_admin']'
};

export type Service2  = {
    // id:number;                          // 1 - realy need ?
    id_user:number;                     // 1
    id_service:string;                // lpu
    list_user_service:string;         // '['lpu_user', 'lpu_admin']'
};
